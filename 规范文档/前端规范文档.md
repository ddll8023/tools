# 前端页面规范

> 本规范已迁移至 skill `frontend-spec`，通过 skill 按需加载对应模块的规范文件，避免全量读取。

## 使用方式

前端开发任务会自动触发 `frontend-spec` skill，skill 会根据任务类型按需加载对应的 reference 文件。

## 规范模块索引

| 模块 | 内容概要 | reference 文件 |
|---|---|---|
| 技术栈 + 组件规范 + 模板结构 | Vue 3/Pinia/Axios/Tailwind、组件目录结构/命名规则（Base/App前缀）、组件库禁用规则、语义化标签、指令缩写、模板组织 | `01-tech-stack-and-template.md` |
| Script 规范 | 组件注释、导入顺序、Props/Emits 定义、Composables、动态 Tailwind 类名 | `02-script.md` |
| Tailwind CSS 规范 | 配置、类名速查、布局模式、状态交互、@apply、自定义 CSS 混用优先级 | `03-tailwind.md` |
| 页面组织与路由 | 目录结构、文件命名、路由懒加载 | `04-page-and-routing.md` |
| API 调用规范 | 模块化组织、Axios 封装、接口定义、snake_case 约定、数据流 | `05-api.md` |
| 错误处理规范 | 错误分类、处理模式、错误/加载状态组件 | `06-error-handling.md` |
| 状态管理 + 性能优化 | Pinia Store 结构、组件懒加载、v-memo、shallowRef、图片优化、safelist | `07-state-and-performance.md` |
| 代码质量 + 图标规范 | 注释/命名规范、Tailwind 排序、ESLint/Prettier、FontAwesome 导入和使用 | `08-quality-and-icons.md` |

## 全局速查（最高频规则）

1. 禁止 Element Plus：项目已全面迁移到自定义组件
2. Composition API：使用 `<script setup>`，不用 Options API
3. 图标统一 FontAwesome：所有图标使用 FontAwesome 6
4. 样式优先 Tailwind：原子类 > `@apply` > 自定义 CSS
5. API 参数 snake_case：请求参数/请求体必须用 snake_case
6. 统一响应格式：`{code, message, data}`，code===0 为成功
7. 路由懒加载：`()` => import()` 动态导入
8. Props 对象语法：defineProps 使用对象语法
9. Emits kebab-case：事件名使用 kebab-case
10. 语义化标签：header/main/section/footer，层级不超过 5 层

## 完整规范路径

skill 目录：`C:\Users\17289\.claude\skills\frontend-spec\`

完整规范原文按功能域拆分存放在 `references/` 子目录下，每个文件可独立阅读。
